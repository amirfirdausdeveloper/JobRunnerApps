<resources>

    <!-- THEME WITH ACTION BAR -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- THEME NO ACTION BAR -->
    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorJR</item>
        <item name="colorPrimaryDark">@color/colorJR</item>
        <item name="colorAccent">@color/colorFont</item>
    </style>

    <!-- THEME FOR DIALOG NOT INTERNET CONNECTION -->
    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
    </style>
    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">#8b8b8b</item>
    </style>
    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorAccent</item>
    </style>
    <!-- END THEME FOR DIALOG NOT INTERNET CONNECTION -->

    <!-- THEME FOR LOGIN -->
    <style name="AppThemeLogin" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorJR</item>
        <item name="colorPrimaryDark">@color/colorJR</item>
        <item name="colorAccent">@color/colorWhite</item>
    </style>
    <!-- END FOR LOGIN -->


    <!-- THEME DIALOG SUCCESS -->
    <style name="CustomDialog" parent="android:Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- THEME DIALOG ACCEPT -->
    <style name="Theme_Dialog" parent="ThemeOverlay.AppCompat.Light" >
        <item name="windowMinWidthMajor">100%</item>
        <item name="windowMinWidthMinor">65%</item>
    </style>

</resources>
